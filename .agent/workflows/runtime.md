---
description: 
---

每次修改完后，执行以下验证步骤：

## 1. 执行部署脚本

运行批处理文件以部署更改：

```powershell
d:\src\177.bat
```

## 2. 打开浏览器验证

使用浏览器打开系统网址，检查是否有错误，只打开这个首页即可，不需点击更多链接检查：

- 访问地址: `http://192.168.30.177`
- 检查页面是否正常加载
- 检查控制台是否有 JavaScript 错误
- 检查网络请求是否有失败的请求

## 3. 报告结果

向用户报告验证结果：

- 部署脚本执行结果
- 页面加载状态
- 发现的任何错误